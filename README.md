# PurpJob

**Инфраструктура профессионального доверия.** Не джоб-борд и не ATS.

Профессиональный опыт не должен начинаться с чистого листа при каждой новой вакансии. Каждый
подтверждённый проект, кейс или пройденное тестовое становится частью профиля и продолжает
работать на кандидата, а не исчезает после отказа.

## Центральная схема продукта

```
Profile  →  Evidence  →  PROF / Trust  →  Match
```

- **Profile** — накопительный контейнер кандидата: хранилище, а не оценка.
- **Evidence** — конкретные доказательства: артефакты, ответы на Contextual Probe, подтверждённые факты.
- **PROF.индекс** — что человек умеет. **Trust Score** — насколько подтверждено то, что о нём известно.
- **Match** — проекция PROF/Trust на конкретную вакансию.

## Статус

**Модуль 1 — начат: форма входа и регистрации по почте.**

Сделан интерфейс формы. Данные пока никуда не отправляются — бэкенда, базы и сессий ещё нет.

| Что есть | Где |
|---|---|
| Мастер-документ, версия 10.16 | [`docs/PurpJob_Master_Document_10_16.md`](docs/PurpJob_Master_Document_10_16.md) |
| Роадмап продуктовых модулей 1–16 | [`docs/roadmap.md`](docs/roadmap.md) |
| Форма входа и регистрации | [`web/index.html`](web/index.html) |
| Локальный сервер для разработки | [`tools/serve.ps1`](tools/serve.ps1) |

### Что умеет форма

- переключение **Вход / Регистрация** без перезагрузки страницы;
- проверка почты и пароля (при регистрации — не короче 8 символов) с понятными сообщениями;
- показать / скрыть пароль; ошибка исчезает, как только поле начали править;
- подписи, `autocomplete` и `aria-invalid` — форма работает с автозаполнением и скринридерами.

**Подтверждение почты не предусмотрено** — после регистрации кандидат сразу попадает в профиль.

### Чего в форме ещё нет

Отправки данных на сервер. Для этого нужен бэкенд, и там появляются решения, которые пока не приняты:
где хранить пользователей (в мастер-документе выбор между pgvector и Pinecone открыт), чем хешировать
пароли, как держать сессию, что делать при восстановлении пароля.

## Как запустить

Один раз после клонирования — установить зависимости:

```bash
npm install
```

Дальше каждый раз:

```bash
npm run dev
```

Браузер открывается сам на **http://localhost:8000**. Любое сохранение файла в `web/`
мгновенно обновляет страницу — перезагружать вручную ничего не нужно. Остановить сервер —
`Ctrl+C` в окне терминала.

### Запасной вариант без Node

Если `node_modules` сломались или Node недоступен, есть сервер на встроенном в Windows
PowerShell — без автообновления, но работает всегда:

```bash
powershell -ExecutionPolicy Bypass -File tools\serve.ps1
```

> **Важно для скриптов `.ps1`:** Windows PowerShell 5.1 читает файлы без BOM как ANSI, из-за чего
> кириллица внутри скрипта превращается в мусор и ломает синтаксис. Все `.ps1` в проекте нужно
> сохранять в **UTF-8 с BOM**.

## Окружение

| Инструмент | Версия | Назначение |
|---|---|---|
| Python | 3.14.7 | будущий бэкенд (FastAPI) |
| Node.js | 24.20.0 | dev-сервер и сборка фронтенда |
| Git | 2.55.0 | версионирование |
| Vite | 7.x | dev-сервер с автообновлением |

## Что дальше

Разработка идёт по **MVP-0** — кандидатский эксперимент без единого рекрутера, модули **1 → 9**:

> профиль → One-Click Enrichment → PROF → белые пятна → Contextual Probe → Trust → экспорт PDF

Модули 10–15 (вакансии, Match, recruiter-side, плагин) подключаются только после первых данных
MVP-0. Модуль 16 — V2. Подробности и обоснование порядка — в [`docs/roadmap.md`](docs/roadmap.md).

Первый сегмент зафиксирован: **Backend/Fullstack, Python/Go, Middle/Senior**.

### Открытые технические вопросы

Стек в мастер-документе (Гл. 14) — карта вариантов, а не финальная спецификация. До найма
технического специалиста не решены:

- Contextual Probe: локальная модель или внешний API;
- векторная БД: pgvector или Pinecone/Milvus, с учётом требований 152-ФЗ;
- конкретная open-weight модель для локального варианта.

Направление, зафиксированное как ориентир: бэкенд — Python (FastAPI/Django), плагин — Chrome
Extension Manifest V3 с ручным выделением текста, без автоскрапинга.
